import { CreateRepositoryDto } from './create-repository.dto';

describe('CreateRepositoryDto', () => {
  it('should be defined', () => {
    expect(new CreateRepositoryDto()).toBeDefined();
  });
});
