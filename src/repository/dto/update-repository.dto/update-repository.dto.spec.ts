import { UpdateRepositoryDto } from './update-repository.dto';

describe('UpdateRepositoryDto', () => {
  it('should be defined', () => {
    expect(new UpdateRepositoryDto()).toBeDefined();
  });
});
